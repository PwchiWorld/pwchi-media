# PWCHIN Landing Page

Current page:

`index.html`

Purpose:

Convert visual interest into action:

**See Pwchies -> Want one -> Download Demo Pack -> Create / Request Guided Tour**

## Local Assets

Images and GIFs used by the static landing page live in:

`landing/img/`

Current assets include:

- clan maps
- pipeline screenshots
- video stills
- hero animation / scan image
- visual proof from existing PWCHIN media

## Download Link

The current primary CTA points to:

`https://pwchin.com/wp-content/uploads/19e5-1/pwchi-demo-pipelines.zip`

The local ZIP copy lives at:

`../demo-pack/pwchi-demo-pipelines.zip`

If the WordPress media URL changes, update the CTA in:

- `landing/index.html`
- `wordpress-drafts/free-pwchi-demo-pack-page.html`

## WordPress Version

WordPress-ready HTML is stored at:

`../wordpress-drafts/free-pwchi-demo-pack-page.html`

The WordPress version should use absolute media URLs from `pwchin.com`, while this static version can use local `img/` paths.

